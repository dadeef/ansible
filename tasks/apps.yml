- name: Install iTerm2
  become_user: "{{ user }}"
  homebrew_cask:
    name: iterm2
    state: present
  tags:
    - apps
      
- name: Install 1password
  become_user: "{{ user }}"
  homebrew_cask:
    name: 1password
    state: present
  tags:
    - apps
